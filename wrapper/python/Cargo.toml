[package]
name = "timslite-python"
version = "0.1.0"
edition = "2021"

[lib]
name = "timslite"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.28", features = ["extension-module"] }
timslite = { path = "../.." }
