[bindings.kotlin]
package_name = "io.github.snower.timslite.uniffi"
cdylib_name = "timslite_java"

# Java 8 compatibility: disable Java 9 Cleaner, rely on explicit close()
disable_java_cleaner = true

[bindings.kotlin.custom_types]
# Custom type mappings can be added here if needed
